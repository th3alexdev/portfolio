{
  "name": "portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 1100",
    "build": "vite build",
    "preview": "vite preview",
    "sass": "sass src/scss/styles.scss src/css/styles.css --no-source-map",
    "postcss": "postcss src/css/styles.css -o src/css/styles.min.css --no-map",
    "build-webapp": "npm run sass && npm run postcss && npm run build"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.23",
    "postcss-cli": "^10.1.0",
    "rollup-plugin-copy": "^3.4.0",
    "sass": "^1.62.0",
    "vite": "^4.1.0"
  }
}
